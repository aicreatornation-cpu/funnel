# Supabase Postgres connection (server-side only — never exposed to the browser)
PGHOST=your-project.pooler.supabase.com
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres.your-project-ref
PGPASSWORD=your-database-password
PORT=3001
