# Base URL of the backend API.
# Leave EMPTY in local dev — Vite proxies /api to the backend (see vite.config.js).
# In production, set this to your deployed backend URL (no trailing slash), e.g.
#   VITE_API_URL=https://time-to-shoot-api.onrender.com
VITE_API_URL=
